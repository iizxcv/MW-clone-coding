name: Git Commit → Notion DB

# main 브랜치에 push될 때 실행
on:
  push:
    branches:
      - main

jobs:
  update-notion:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Git 레포 체크아웃
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2  # 최근 커밋 2개 가져오기

      # 2️⃣ jq 설치 (JSON 파싱용)
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      # 3️⃣ 최신 커밋 정보 가져오기
      - name: Get last commit info
        id: get_commit
        run: |
          # Git log에서 SHA, 작성자, 제목, 본문, ISO 8601 날짜 가져오기
          git log -1 --pretty=format:'%H%n%an%n%s%n%b%n%cI' > last_commit.txt

          # 배열로 읽기
          readarray -t arr < last_commit.txt
          COMMIT_SHA="${arr[0]}"
          AUTHOR="${arr[1]}"
          TITLE="${arr[2]}"
          DATE_ISO="${arr[-1]}"

          # 본문(body) 안전하게 파일로 저장 (멀티라인, 줄바꿈, 한글 가능)
          printf "%s\n" "${arr[@]:3:${#arr[@]}-4}" > commit_body.txt

          # 환경변수로 필요한 값 설정
          echo "COMMIT_SHA=$COMMIT_SHA" >> $GITHUB_ENV
          echo "AUTHOR=$AUTHOR" >> $GITHUB_ENV
          echo "TITLE=$TITLE" >> $GITHUB_ENV
          echo "DATE_ISO=$DATE_ISO" >> $GITHUB_ENV

          echo "Commit date ISO: $DATE_ISO"
          echo "Commit body saved to commit_body.txt"

      # 4️⃣ Notion DB에 커밋 기록 추가
      - name: Add commit to Notion DB
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}             # Notion 통합 토큰
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }} # Notion DB ID
          SHA: ${{ env.COMMIT_SHA }}
          AUTHOR: ${{ env.AUTHOR }}
          TITLE: ${{ env.TITLE }}
          DATE_ISO: ${{ env.DATE_ISO }}
        run: |
          # commit_body.txt 읽어서 JSON 안전 문자열로 변환
          BODY_JSON=$(jq -Rs '.' < commit_body.txt)

          # Notion 페이지 생성용 JSON
          JSON_PAYLOAD=$(jq -n \
            --arg title "$TITLE" \
            --arg sha "$SHA" \
            --arg author "$AUTHOR" \
            --arg date_iso "$DATE_ISO" \
            --argjson body "$BODY_JSON" \
            '{
              parent: { database_id: "'$NOTION_DATABASE_ID'" },
              properties: {
                "Title": { "title": [{ "text": { "content": $title } }] },
                "Body": { "rich_text": [{ "text": { "content": $body } }] },
                "Commit SHA": { "rich_text": [{ "text": { "content": $sha } }] },
                "Author": { "rich_text": [{ "text": { "content": $author } }] },
                "Commit Date": { "date": { "start": $date_iso } }
              }
            }')

          # Notion API 호출
          response=$(echo "$JSON_PAYLOAD" | curl -s -X POST "https://api.notion.com/v1/pages" \
            -H "Authorization: Bearer $NOTION_TOKEN" \
            -H "Notion-Version: 2022-06-28" \
            -H "Content-Type: application/json" \
            -d @-)

          # API 응답 출력 (디버깅용)
          echo "Notion API response: $response"
